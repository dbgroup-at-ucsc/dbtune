<?xml version="1.0"?>
<project name="dbtune-ml-tasks" basedir="." default="compile">

  <import file="../../common.xml"/>

  <path id="ml.compile.classpath">
    <fileset dir="../../${lib.dir}" includes="*.jar" />
    <fileset dir="${lib.dir}" includes="*.jar" />
    <pathelement location="${build.dir}/classes/" />
  </path>

  <target name="init">
    <taskdef resource="scala/tools/ant/antlib.xml">
      <classpath refid="ml.compile.classpath"/>
    </taskdef>
  </target>

  <!-- this depends on running 'ant compile' from dbtune root manually -->
  <target name="compile" depends="init" description="Compile Scala source">
    <scalac srcdir="${src.dir}" destdir="${build.dir}/classes" classpathref="ml.compile.classpath"><!-- addParams="-verbose"> -->
      <include name="**/*.scala" />
    </scalac>
  </target>

  <target name="compile.tests" depends="compile" description="Compile test source">
    <scalac srcdir="${test.dir}" destdir="${build.dir}/tests" classpathref="ml.compile.classpath" ><!-- addParams="-verbose"> -->
      <include name="**/*.scala" />
    </scalac>
  </target>

  <target name="test.functional" depends="compile.tests" description="Execute functional tests">
  </target>

  <target name="test.unit" depends="compile.tests" description="Execute tests">
  </target>
</project>
